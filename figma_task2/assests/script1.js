function toggleMenu() {
    const navbar = document.querySelector('.section1_navbar');
    const menu = document.querySelector('.menu');
    navbar.classList.toggle('active'); // Toggle navbar visibility
    menu.classList.toggle('active'); // Toggle menu visibility
}
